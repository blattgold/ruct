# RUCT: imitation (C)rab.
This is a 'standard library' implementation for Rust-Like Syntax and semantics in C.

## Goal
Single-Header library that is compatible with any C-Compiler that allows usage of Rust-like features in C.

*Work in progress, many features missing and half-finished.*
